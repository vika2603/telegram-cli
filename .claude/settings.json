{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.go) if command -v goimports >/dev/null 2>&1; then goimports -local github.com/vika2603/telegram-cli -w \"$f\"; else gofmt -w \"$f\"; fi ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
